import { Form, HeroTitle, Mask } from './styles'

const Hero = () => (
  <Form>
    <Mask>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Mask>
  </Form>
)

export default Hero
